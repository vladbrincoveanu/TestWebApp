@model Author


<div class="col-sm-4 col-lg-4 col-md-4">
    <div class="panel panel-primary">
        <div class="panel-heading">
            <p>@Model.Name</p>
        </div>
        <div class="panel-body">
            <h2>Game count: @Model.Game?.Count </h2>
            <div class="btn-group">
                @if (Model.Game?.Count == 0)
                {
                    <a class="btn btn-default" asp-area="">View game list</a>
                }
                else
                {
                    <a class="btn btn-info" asp-area="" asp-controller="Game" asp-action="List" asp-route-authorId="@Model.AuthorId">View game list</a>
                }
                <a class="btn btn-info" asp-area="" asp-controller="Author" asp-action="Update" asp-route-id="@Model.AuthorId">Edit</a>
                <a class="btn btn-danger" asp-area="" asp-controller="Author" asp-action="Delete" asp-route-id="@Model.AuthorId">Delete</a>
            </div>
        </div>
    </div>
</div>